# Usage

    cp .env.example .env
    // Populate your tokens and user ids
    composer install
    ($ chmod +x ./run.sh) // Optional if not runnable
    $ ./run.sh
    // Add to slack bots
    // invite to channel
    !help // Into slack channel
# Requirements
php7+
